{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Server-specific options
    "module": "commonjs",
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "target": "ES2023",
    "outDir": "../../dist", // Specific to server output
    "baseUrl": "./" // Relative to this tsconfig.json
  }
}
